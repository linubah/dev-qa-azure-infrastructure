# Terraform
.terraform/
.terraform.tfstate.lock.info
*.tfstate
*.tfstate.*
*.tfplan
tfplan
crash.log
crash.*.log

# Variable files may carry secrets - env roots commit their own explicitly.
*.auto.tfvars
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Lockfiles belong to root modules only, never shared modules.
modules/**/.terraform.lock.hcl

# Local
.DS_Store
*.bak
/temp